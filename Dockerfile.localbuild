# Build JAR on host first: ./gradlew bootJar
# Then: podman build -f Dockerfile.localbuild -t padix-api:latest .
FROM eclipse-temurin:21-jre
WORKDIR /app
COPY build/libs/app.jar /app/app.jar
EXPOSE 8080
ENTRYPOINT ["java","-jar","/app/app.jar"]
