# Build JAR on host first: ./gradlew bootJar
# Then: podman build -f Dockerfile.localbuild -t padelgo-api:latest .
FROM eclipse-temurin:17-jre
WORKDIR /app
COPY build/libs/app.jar /app/app.jar
EXPOSE 8080
ENTRYPOINT ["java","-jar","/app/app.jar"]
