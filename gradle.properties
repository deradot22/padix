org.gradle.jvmargs=-Xmx6g -XX:MaxMetaspaceSize=2g -Dfile.encoding=UTF-8
kotlin.compiler.execution.strategy=in-process
# Запускаем модули последовательно, чтобы не было двух Kotlin compile-задач параллельно
# (это удваивает потребление Metaspace и приводит к OOM).
org.gradle.parallel=false
