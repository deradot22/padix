// Root project Padix. Никакого кода в корне — только подмодули `api` и `bot`.
// Общие плагины и repositories конфигурируются внутри каждого модуля.

allprojects {
    repositories {
        mavenCentral()
    }
}
